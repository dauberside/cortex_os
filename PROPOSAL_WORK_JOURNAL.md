# 業務日誌・勤怠管理機能 実装提案書

**作成日**: 2026-02-15
**更新日**: 2026-02-15
**ステータス**: 提案中（未承認）

## 概要

現在のノート管理システムを業務日誌形態に拡張し、夜勤・日跨ぎ勤務に対応した勤怠管理機能を追加する提案です。

## 要件

### 夜勤対応の要件
- 日跨ぎ勤務に対応（例：2/15 15:30 ～ 2/16 9:30）
- 勤務開始時刻・終了時刻を記録
- 実働時間の自動計算
- 休憩時間の記録（複数回対応）
- 勤務日の基準を「開始日」とする（2/15の勤務として記録）

---

## 提案オプション

### オプションA: 勤怠管理対応版（推奨）

**概要**: 夜勤・日跨ぎ勤務に対応した業務日誌＋勤怠管理機能

**機能**:
- 勤務日（基準日）の設定
- 勤務開始時刻・終了時刻の記録（日跨ぎ対応）
- 休憩時間の記録（複数回対応）
- 実働時間の自動計算
- 勤怠統計（週次/月次の合計時間）
- 業務日誌テンプレート

**実装内容**:
1. データベーススキーマ変更
   ```prisma
   model Note {
     // ... 既存フィールド
     workDate DateTime? // 勤務日（基準日）
     startTime DateTime? // 勤務開始時刻
     endTime DateTime? // 勤務終了時刻
     isJournal Boolean @default(false) // 業務日誌フラグ
   }

   // 休憩時間記録用の新しいモデル
   model BreakTime {
     id String @id @default(cuid())
     noteId String
     note Note @relation(fields: [noteId], references: [id], onDelete: Cascade)
     startTime DateTime
     endTime DateTime
     createdAt DateTime @default(now())

     @@index([noteId])
   }
   ```

2. UI追加
   - 「業務日誌を作成」ボタン
   - 勤務日選択（デフォルト：今日）
   - 勤務開始・終了時刻入力
   - 休憩時間入力（追加/削除可能）
   - 実働時間の自動表示
   - 勤怠統計ダッシュボード

3. テンプレート
   ```markdown
   ## 勤務情報
   - 勤務日: 2026-02-15
   - 開始: 2026-02-15 15:30
   - 終了: 2026-02-16 09:30
   - 休憩: 1時間（22:00-23:00）
   - 実働: 17時間

   ## 本日の作業内容
   -

   ## 成果・進捗
   -

   ## 課題・問題点
   -

   ## 次回の予定
   -

   ## 備考
   ```

4. 勤怠計算ロジック
   ```typescript
   // 実働時間 = (終了時刻 - 開始時刻) - 休憩時間合計
   // 例: (2/16 9:30 - 2/15 15:30) - 1h = 18h - 1h = 17h
   ```

**メリット**:
- 夜勤・日跨ぎ勤務に完全対応
- 勤怠管理と業務記録を一元化
- 統計情報で労働時間を可視化
- 将来的な給与計算との連携が可能

**デメリット**:
- 実装時間が増加（3-4時間）
- データモデルが複雑化

---

### オプションB: フル機能版（エンタープライズ対応）

**概要**: 本格的な勤怠管理・業務日誌システム

**機能**:
- オプションAの全機能
- カレンダービュー（月間/週間）
- 勤怠レポート自動生成（CSVエクスポート対応）
- 残業時間の自動計算（設定した所定労働時間との差分）
- 深夜勤務時間の自動判定（22:00-5:00）
- 業務カテゴリタグ（開発/会議/レビュー/調査等）
- ダッシュボード（統計グラフ表示）
- 勤怠承認フロー（将来的にマネージャー承認機能）

**実装内容**:
1. データベーススキーマ拡張
   ```prisma
   model Note {
     // ... 既存フィールド
     workDate DateTime?
     startTime DateTime?
     endTime DateTime?
     category String?
     isJournal Boolean @default(false)
     isApproved Boolean @default(false) // 承認フラグ
     approvedAt DateTime?
   }

   model BreakTime {
     id String @id @default(cuid())
     noteId String
     note Note @relation(fields: [noteId], references: [id], onDelete: Cascade)
     startTime DateTime
     endTime DateTime
     createdAt DateTime @default(now())
   }

   model WorkSettings {
     id String @id @default(cuid())
     userId String @unique
     regularHours Int @default(8) // 所定労働時間
     nightShiftStart Int @default(22) // 深夜開始時刻
     nightShiftEnd Int @default(5) // 深夜終了時刻
   }
   ```

2. 新規ページ追加
   - `/journal` - 業務日誌専用ページ
   - `/attendance` - 勤怠管理ダッシュボード
   - カレンダーコンポーネント（勤務日表示）
   - タイムライン表示
   - グラフ表示（月次勤務時間推移等）

3. ライブラリ追加
   - `react-calendar` - カレンダーUI
   - `recharts` - グラフ表示
   - `date-fns` - 高度な日付計算

4. 高度な計算機能
   ```typescript
   // 実働時間計算
   // 残業時間計算（所定労働時間超過分）
   // 深夜勤務時間計算（22:00-5:00の時間）
   // 週次/月次統計
   ```

**メリット**:
- 本格的な勤怠管理が可能
- 法定労働時間管理に対応
- データ分析・可視化
- 給与計算システムとの連携が容易
- 長期的に非常に有用

**デメリット**:
- 実装時間が長い（6-8時間）
- システムが複雑化
- 依存ライブラリ増加

---

### オプションC: 最小限

**概要**: データベース変更なしで実装

**機能**:
- 新規作成時に「業務日誌テンプレート」ボタン
- クリックでテンプレートを本文に挿入
- タイトルに日付を自動挿入

**実装内容**:
1. UIのみ変更
   - 新規作成フォームにテンプレートボタン追加
   - JavaScriptでテンプレート文字列を挿入

2. データベース変更なし

**メリット**:
- 最も簡単（30分程度）
- リスクゼロ
- すぐに使える

**デメリット**:
- 日付ベースの検索・ソートができない
- 単なるテンプレート機能のみ
- 拡張性なし

---

## 推奨案

**オプションA（勤怠管理対応版）を推奨**

理由:
1. 夜勤・日跨ぎ勤務に完全対応
2. 実装コストと機能のバランスが良い（3-4時間）
3. 既存システムへの影響が最小限
4. 将来的にオプションBへ拡張可能
5. 勤怠管理の基本機能を網羅

---

## 実装スケジュール（オプションA）

1. **データベーススキーマ変更** (30分)
   - Prismaスキーマに `workDate`, `startTime`, `endTime`, `isJournal` フィールド追加
   - `BreakTime` モデル追加
   - マイグレーション実行

2. **バックエンド実装** (60分)
   - tRPCルーターに勤怠関連エンドポイント追加
     - 業務日誌作成（休憩時間含む）
     - 勤怠統計取得（日次/週次/月次）
   - 実働時間計算ロジック実装
   - 休憩時間CRUD実装

3. **フロントエンド実装** (90分)
   - 業務日誌作成UI
     - 勤務日選択
     - 開始/終了時刻入力（日付＋時刻）
     - 休憩時間入力（複数対応）
     - 実働時間の自動表示
   - テンプレート挿入ロジック
   - 勤怠統計表示コンポーネント
   - 日跨ぎ勤務の視覚的表示

4. **テスト・調整** (40分)
   - 夜勤パターンの動作確認
   - 実働時間計算の検証
   - UI調整
   - エラーハンドリング

**合計**: 約3.5時間

---

## UIモックアップ（オプションA）

### 業務日誌作成フォーム

```
┌─────────────────────────────────────────────────┐
│ 業務日誌を作成                                   │
├─────────────────────────────────────────────────┤
│                                                 │
│ 勤務日（基準日）                                 │
│ ┌───────────────┐                             │
│ │ 2026-02-15 ▼ │  ← 夜勤の開始日               │
│ └───────────────┘                             │
│                                                 │
│ 勤務時間                                         │
│ ┌────────────────┬────────────────┐           │
│ │ 開始時刻       │ 終了時刻       │           │
│ ├────────────────┼────────────────┤           │
│ │ 2026-02-15     │ 2026-02-16     │           │
│ │ 15:30          │ 09:30          │           │
│ └────────────────┴────────────────┘           │
│                                                 │
│ 休憩時間                                         │
│ ┌────────────────┬────────────────┬────┐      │
│ │ 開始           │ 終了           │    │      │
│ ├────────────────┼────────────────┼────┤      │
│ │ 2026-02-15     │ 2026-02-15     │ ×  │      │
│ │ 22:00          │ 23:00          │    │      │
│ └────────────────┴────────────────┴────┘      │
│ ［+ 休憩を追加］                                 │
│                                                 │
│ 実働時間: 17時間 （自動計算）                     │
│                                                 │
│ ─────────────────────────────────────────────   │
│                                                 │
│ タイトル                                         │
│ ┌─────────────────────────────────┐           │
│ │ 2026-02-15 業務日誌              │           │
│ └─────────────────────────────────┘           │
│                                                 │
│ 業務内容                                         │
│ ┌─────────────────────────────────┐           │
│ │ ## 勤務情報                      │           │
│ │ - 勤務日: 2026-02-15             │           │
│ │ - 開始: 2026-02-15 15:30         │           │
│ │ - 終了: 2026-02-16 09:30         │           │
│ │ - 休憩: 1時間                    │           │
│ │ - 実働: 17時間                   │           │
│ │                                 │           │
│ │ ## 本日の作業内容                │           │
│ │ -                               │           │
│ └─────────────────────────────────┘           │
│                                                 │
│ タグ                                             │
│ [ 業務日誌 ] [ 夜勤 ] [ 開発 ] ...              │
│                                                 │
│ ［作成］ ［キャンセル］                          │
└─────────────────────────────────────────────────┘
```

### 勤怠統計ダッシュボード

```
┌─────────────────────────────────────┐
│ 勤怠統計                             │
├─────────────────────────────────────┤
│                                     │
│ 【今週】 2026/02/10 - 02/16         │
│ ┌─────────────────────────────┐   │
│ │ 総実働時間: 35時間              │   │
│ │ 勤務日数: 5日                  │   │
│ │ 平均実働: 7時間/日             │   │
│ └─────────────────────────────┘   │
│                                     │
│ 【今月】 2026年2月                  │
│ ┌─────────────────────────────┐   │
│ │ 総実働時間: 120時間             │   │
│ │ 勤務日数: 18日                 │   │
│ │ 夜勤回数: 6回                  │   │
│ └─────────────────────────────┘   │
│                                     │
│ 【直近の勤務】                       │
│ 02/15 (木) 17h [夜勤]              │
│ 02/14 (水) 8h                      │
│ 02/13 (火) 9h                      │
│                                     │
│ ［CSVエクスポート］                  │
└─────────────────────────────────────┘
```

---

## データ移行戦略

**既存ノートへの影響**: なし

- 既存のノートは `date` が NULL のまま
- 業務日誌として作成したノートのみ `date` が設定される
- 日付ソートは日付があるノート優先、その後は通常のソート

---

## 次のステップ

1. ユーザー承認を取得
2. 選択されたオプションで実装開始
3. レビュー・フィードバック
4. 本番環境へデプロイ

---

## 実装例: 実働時間計算ロジック

```typescript
// 実働時間の計算
function calculateWorkHours(
  startTime: Date,
  endTime: Date,
  breaks: { startTime: Date; endTime: Date }[]
): number {
  // 総勤務時間（ミリ秒）
  const totalMs = endTime.getTime() - startTime.getTime();

  // 休憩時間の合計（ミリ秒）
  const breakMs = breaks.reduce((sum, b) => {
    return sum + (b.endTime.getTime() - b.startTime.getTime());
  }, 0);

  // 実働時間（時間単位）
  const workHours = (totalMs - breakMs) / (1000 * 60 * 60);

  return Math.round(workHours * 10) / 10; // 小数点1桁で四捨五入
}

// 使用例
const start = new Date("2026-02-15T15:30:00");
const end = new Date("2026-02-16T09:30:00");
const breaks = [
  {
    startTime: new Date("2026-02-15T22:00:00"),
    endTime: new Date("2026-02-15T23:00:00"),
  },
];

const hours = calculateWorkHours(start, end, breaks);
// => 17.0 時間
```

## 勤怠データのエクスポート例（CSV）

```csv
勤務日,開始時刻,終了時刻,休憩時間,実働時間,業務内容
2026-02-15,2026-02-15 15:30,2026-02-16 09:30,1.0,17.0,"夜勤シフト。システムメンテナンス実施。"
2026-02-14,2026-02-14 09:00,2026-02-14 18:00,1.0,8.0,"通常勤務。新機能開発。"
2026-02-13,2026-02-13 09:00,2026-02-13 18:30,1.0,8.5,"通常勤務。バグ修正対応。"
```

## 補足: 将来的な拡張案

オプションAを実装後、ユーザーフィードバックに応じて以下を追加可能:

- カレンダービュー（月間カレンダーで勤務日を可視化）
- 残業時間の自動計算（所定労働時間との差分）
- 深夜勤務時間の自動判定（22:00-5:00）
- 給与計算補助機能（時給設定、概算給与表示）
- テンプレートのカスタマイズ機能
- 複数テンプレート対応（開発日誌/会議記録/調査報告等）
- 勤怠承認フロー（上長承認機能）
- 週次/月次レポートの自動生成とメール送信

---

## 注意事項

### 夜勤における日付の扱い

- **勤務日（基準日）**: 勤務開始日を基準とする
  - 例：2/15 15:30 ～ 2/16 9:30 → 「2/15の勤務」として記録
- **統計計算**: 勤務日基準で集計
  - 週次統計：勤務日が属する週でカウント
  - 月次統計：勤務日が属する月でカウント

### 休憩時間の制限

- 休憩時間は勤務時間内に含まれる必要がある
- 休憩の開始・終了が勤務時間外の場合はエラー表示
- 休憩時間の重複チェック

---

**作成者**: Claude Code
**承認待ち**: ユーザー
**次のアクション**: オプション選択後、実装開始

# 重度障害向け 業務日誌（インシデントログ）機能 実装提案書

**作成日**: 2026-02-16
**更新日**: 2026-02-16
**ステータス**: 提案中（未承認）

## 概要

現行のノート管理システム（Note/Tag/Link/AIMessage）を、**重度障害（Major Incident / P0-P1）対応の「インシデントログ（業務日誌）」**として拡張し、
- その場の対応（記録・連絡・タイムライン・判断）
- 事後の振り返り（ポストモーテム／再発防止）
- 証跡保全（監査・セキュリティ・説明責任）

を一気通貫で支える仕組みを構築します。

本提案は「夜勤・日跨ぎ勤怠」ではなく、**重大障害対応に最適化した“業務日誌”**へ要件を書き換えたものです。

---

## 背景と課題

重度障害対応では、次が同時進行になります。

- 状況把握（何が壊れているか／影響はどこまでか）
- 役割分担（指揮・コミュニケーション・現場対応）
- タイムライン管理（いつ何をしたか／判断根拠）
- 社内外連絡（定期アップデート、経営・CS向け共有）
- 証跡（ログ・スクショ・作業記録・意思決定）
- 事後の学習（根本原因・再発防止）

現状の「ノート」だけでは、
- タイムラインが散らばる
- 役割と責任が曖昧になる
- 連絡履歴や意思決定の根拠が追いにくい
- ポストモーテムに必要な情報が欠落しやすい

という問題が起きます。

---

## ゴール（非ゴール）

### ゴール
- **重度障害の“対応ログ”が一枚で完結**（タイムライン＋更新＋アクション＋証跡）
- **役割（IC/Comms/SME）と意思決定の文脈を残す**
- **復旧後にポストモーテムへスムーズに移行**（RCA/再発防止の作成を支援）
- **監査・説明責任に耐える記録保全**（改ざん防止の方針、保存形式）

### 非ゴール（MVPではやらない）
- 外部ステータスページへの自動投稿
- Slack/Teams/Jira/PagerDuty 等との完全自動同期
- 監視システムとの自動検知・自動起票

---

## 用語
- **インシデント（Incident）**: サービスの中断や品質低下など、対応が必要な事象
- **重度障害（Major Incident）**: 重大な顧客影響・広範囲影響があり、専任の指揮が必要な事象
- **IC（Incident Commander）**: 指揮・優先順位・意思決定
- **Comms（Communication Lead）**: 社内外コミュニケーション、定期アップデート
- **SME（Subject Matter Expert）**: 技術的調査・復旧作業

---

## 要件

### 1. 重度障害対応に必要な要件（MVP）

#### 1-1. インシデント作成・状態遷移
- インシデント作成（タイトル、重大度、影響サービス、開始時刻）
- 状態遷移：`Detected → Investigating → Mitigated → Resolved → Postmortem`
- 主要時刻：`detectedAt / mitigatedAt / resolvedAt` を保持

#### 1-2. タイムライン（行動ログ）
- 「いつ／誰が／何をしたか」をイベントとして記録
- イベント種別：`Action`（作業）/ `Decision`（判断）/ `Update`（状況共有）/ `Evidence`（証跡）

#### 1-3. 役割と担当者
- 役割割当：IC / Comms / SME（複数）
- 途中交代（ハンドオフ）を記録（引継ぎメモ）

#### 1-4. 影響範囲と現況サマリ
- 影響（Impact）：ユーザー影響、SLO影響、影響サービス、リージョン等
- 現況（Current Status）：いま何が分かっていて、次に何をするか

#### 1-5. 定期アップデート（社内外向け）
- 5〜15分間隔などの更新をテンプレで作成
- 更新内容：現況、次のアップデート予定時刻、暫定回避策

#### 1-6. ポストモーテム支援（MVP）
- RCAの骨子：原因（仮説→確定）、検知、復旧、再発防止タスク
- アクションアイテム（Owner/期限/ステータス）

#### 1-7. 証跡保全
- 重要なログ・リンク・スクショ・Runbook参照先を「証跡」として紐付け
- 監査向けに、時系列と更新者を保持

---

### 2. 既存機能との統合

- **Note**: インシデント本体（Incident Note）として扱える（テンプレ＋構造化メタデータ）
- **Tag**: `P0/P1`, `region/us-east`, `db`, `network` 等の分類
- **Link**: 関連ノート（手順書、障害メモ、原因調査ノート）や関連インシデントへのリンク
- **AIMessage**: 事後にRCAの下書き、タイムライン要約、更新文面の生成を支援（後続フェーズ）

---

## 提案オプション

### オプションA: インシデントログ最適化版（推奨）

**概要**: 既存Noteを活かしつつ、重度障害に必要な構造化データを追加して「ログとして運用可能」にする。

#### データベース（Prisma）案

```prisma
// Noteを「インシデント本体」としても使えるように拡張
model Note {
  // ...既存フィールド
  isIncident   Boolean   @default(false)
  severity     String?   // P0/P1/P2...
  incidentStatus String? // Detected/Investigating/Mitigated/Resolved/Postmortem
  detectedAt   DateTime?
  mitigatedAt  DateTime?
  resolvedAt   DateTime?
  affectedServices String[] @default([])
  impactSummary String?
  commsChannelUrl String?
}

// タイムラインイベント（行動ログ）
model IncidentEvent {
  id        String   @id @default(cuid())
  noteId    String
  note      Note     @relation(fields: [noteId], references: [id], onDelete: Cascade)

  type      String   // Action/Decision/Update/Evidence
  message   String
  createdAt DateTime @default(now())

  actorUserId String
  actor     User     @relation(fields: [actorUserId], references: [id], onDelete: Cascade)

  @@index([noteId, createdAt])
  @@index([actorUserId, createdAt])
}

// 役割割当（IC/Comms/SME）
model IncidentRoleAssignment {
  id        String   @id @default(cuid())
  noteId    String
  note      Note     @relation(fields: [noteId], references: [id], onDelete: Cascade)

  role      String   // IC/Comms/SME
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  assignedAt DateTime @default(now())
  endedAt    DateTime?

  @@index([noteId, role])
  @@index([userId])
}

// 再発防止タスク
model IncidentActionItem {
  id        String   @id @default(cuid())
  noteId    String
  note      Note     @relation(fields: [noteId], references: [id], onDelete: Cascade)

  title     String
  ownerUserId String
  owner     User     @relation(fields: [ownerUserId], references: [id], onDelete: Cascade)

  dueAt     DateTime?
  status    String   @default("Open") // Open/InProgress/Done
  createdAt DateTime @default(now())

  @@index([noteId, status])
  @@index([ownerUserId, status])
}
```

#### UI（MVP）
- インシデント作成（テンプレ付き）
- インシデント詳細ページ
  - 状態・重大度・影響サマリ
  - 役割割当（IC/Comms/SME）
  - タイムライン（イベント追加・編集）
  - 定期アップデート（テンプレ生成）
  - アクションアイテム

#### メリット
- 重度障害の“運用”に必要な構造が揃う
- 既存Note/Tag/Linkを活かして拡張できる
- 後から外部連携（Slack/Jira等）へ育てやすい

#### デメリット
- DBスキーマが増える（ただし分離されているため安全）

---

### オプションB: フル機能版（外部連携前提）

- Slack/Teamsスレッド同期
- インシデント更新の定期送信
- Jira/GitHub Issue化（Action Item同期）
- ステータスページ連携

---

### オプションC: 最小限（DB変更なし）

- 「インシデントテンプレート」ボタンでノート本文にテンプレを挿入
- すぐ使えるが、タイムラインや役割、アクションの構造化ができない

---

## 推奨案

**オプションA（インシデントログ最適化版）** を推奨します。

- 重度障害対応で重要な「役割」「タイムライン」「コミュニケーション」「証跡」「事後タスク」を、
  最小限の構造化で確実に残せる
- 既存の Note/Tag/Link と自然に統合でき、実装リスクが低い

---

## 実装スケジュール（オプションA）

1. **DBスキーマ拡張**（45〜60分）
   - Note拡張（incident関連フィールド）
   - IncidentEvent / IncidentRoleAssignment / IncidentActionItem の追加
   - マイグレーション

2. **バックエンド（tRPC）**（90分）
   - incident.create / incident.get / incident.updateStatus
   - event.add / event.list
   - role.assign / role.end
   - actionItem.create / list / update

3. **フロントエンド**（120分）
   - インシデント一覧
   - インシデント詳細（タイムライン、更新、役割、アクション）
   - テンプレ挿入

4. **テスト・調整**（45分）
   - 状態遷移、権限制御、日跨ぎ時刻の表示確認

**合計**: 約5時間（MVP）

---

## テンプレート（本文に挿入する初期フォーマット例）

```markdown
# [P0] インシデント概要
- 影響サービス: 
- 影響（ユーザー/SLO）: 
- 検知: いつ/どこで/誰が
- 現況: 
- 次のアップデート: 

## 役割
- IC: 
- Comms: 
- SME: 

## タイムライン
- YYYY-MM-DD HH:MM 検知（誰が）
- YYYY-MM-DD HH:MM 初動（何をした）
- YYYY-MM-DD HH:MM 判断（根拠）
- YYYY-MM-DD HH:MM 緩和（Mitigated）
- YYYY-MM-DD HH:MM 復旧（Resolved）

## 暫定対応（ワークアラウンド）
-

## 根本原因（RCA）
- 仮説:
- 確定:

## 再発防止（Action Items）
- [ ] (Owner) タスク / 期限

## 証跡
- ログリンク:
- ダッシュボード:
- 変更/デプロイ:
```

---

## データ移行方針

- 既存ノートへの影響：なし（incident関連はNULL/false）
- インシデントとして作成したノートのみ `isIncident=true` を設定

---

**作成者**: （ユーザー）
**次のアクション**: オプション選択（推奨A）→ 実装開始
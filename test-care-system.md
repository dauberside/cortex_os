# 重度障害者支援システム - 動作確認手順

## 実装完了した機能

### ✅ バックエンド（tRPCルーター）

1. **recipient** - 利用者管理
   - `list` - 利用者一覧取得
   - `get` - 利用者詳細取得
   - `create` - 利用者作成
   - `update` - 利用者更新
   - `delete` - 利用者削除（論理削除）
   - `stats` - 統計情報取得

2. **careRecord** - 支援記録
   - `list` - 支援記録一覧取得
   - `get` - 支援記録詳細取得
   - `create` - 支援記録作成
   - `update` - 支援記録更新
   - `delete` - 支援記録削除
   - `statsByCategory` - カテゴリ別統計

3. **vitalSign** - バイタルサイン
   - `list` - バイタルサイン一覧取得
   - `get` - バイタルサイン詳細取得
   - `create` - バイタルサイン作成
   - `update` - バイタルサイン更新
   - `delete` - バイタルサイン削除
   - `stats` - 統計情報取得（グラフ用）
   - `checkAbnormal` - 異常値チェック

4. **medication** - 服薬管理
   - `list` - 服薬記録一覧取得
   - `get` - 服薬記録詳細取得
   - `create` - 服薬記録作成
   - `update` - 服薬記録更新
   - `delete` - 服薬記録削除
   - `statsByStatus` - ステータス別統計

5. **handover** - 申し送り
   - `list` - 申し送り一覧取得
   - `get` - 申し送り詳細取得
   - `create` - 申し送り作成
   - `update` - 申し送り更新
   - `confirm` - 申し送り確認
   - `delete` - 申し送り削除
   - `unconfirmedCount` - 未確認件数取得
   - `overdue` - 期限切れ申し送り取得

6. **careIncident** - ヒヤリハット・事故報告
   - `list` - ヒヤリハット一覧取得
   - `get` - ヒヤリハット詳細取得
   - `create` - ヒヤリハット作成
   - `update` - ヒヤリハット更新
   - `delete` - ヒヤリハット削除
   - `statsByType` - タイプ別統計
   - `statsBySeverity` - 重要度別統計
   - `monthlyTrend` - 月次推移統計

### ✅ フロントエンド

1. **ダッシュボード** (`/dashboard`)
   - 統計カード表示
   - 機能メニュー
   - クイックアクション

2. **利用者管理** (`/recipients`)
   - 利用者一覧表示
   - 統計情報表示
   - 削除済み利用者の表示切り替え

3. **利用者登録** (`/recipients/new`)
   - 基本情報入力
   - 障害情報入力
   - 連絡先・医療情報入力
   - バリデーション（Zod）

## 動作確認手順

### 1. 開発サーバーの起動確認

```bash
# サーバーが起動していることを確認
# http://localhost:3000 でアクセス可能
```

### 2. 認証

1. ブラウザで `http://localhost:3000` にアクセス
2. サインインページにリダイレクトされる
3. 認証情報を入力してログイン

### 3. ダッシュボード確認

1. `/dashboard` にアクセス
2. 以下が表示されることを確認：
   - 統計カード（利用者数、未確認申し送りなど）
   - 機能メニュー（利用者管理、支援記録など）
   - クイックアクション
   - 開発状況

### 4. 利用者登録

1. ダッシュボードから「新規利用者登録」ボタンをクリック
2. `/recipients/new` に遷移
3. 以下の情報を入力：
   - **基本情報**
     - 氏名: 山田 太郎
     - 氏名（カナ）: ヤマダ タロウ
     - 生年月日: 1990-01-01
     - 性別: 男性
   - **障害情報**
     - 障害種別: 身体障害にチェック
     - 障害支援区分: 区分6
   - **連絡先・医療情報**
     - 緊急連絡先: 090-1234-5678
     - 主治医: 田中医師
     - 医療機関: ○○総合病院
     - アレルギー情報: 卵アレルギー
4. 「利用者を登録」ボタンをクリック
5. 登録が成功することを確認

### 5. 利用者一覧確認

1. `/recipients` にアクセス
2. 登録した利用者が一覧に表示されることを確認
3. 統計カード（総利用者数、障害支援区分別）が更新されていることを確認
4. 利用者カードをクリックして詳細画面に遷移できることを確認

### 6. エラーハンドリング確認

1. `/recipients/new` で必須項目を空のまま登録
2. Zodバリデーションエラーが表示されることを確認
3. エラーメッセージが日本語で表示されることを確認

## 今後の実装予定

### Phase 2

- [ ] 利用者詳細画面
  - 利用者情報の編集
  - 支援記録の表示
  - バイタルサイン履歴
  - 服薬記録
  - 申し送り一覧
  - ヒヤリハット一覧

- [ ] 支援記録入力画面
  - カテゴリ別入力フォーム
  - テンプレート機能
  - 記録一覧・検索

- [ ] バイタルサイン記録・グラフ
  - 測定値入力
  - グラフ表示（日次/週次/月次）
  - 異常値アラート

- [ ] 申し送り管理
  - 申し送り作成・一覧
  - 未確認アラート
  - 確認機能

- [ ] ヒヤリハット報告
  - 報告フォーム
  - 統計・分析
  - 再発防止策管理

### Phase 3

- [ ] 個別支援計画管理
- [ ] 検索・フィルタ機能の強化
- [ ] CSVエクスポート
- [ ] 勤務管理（夜勤・日跨ぎ対応）
- [ ] モバイル対応の最適化

## 技術スタック確認

### データベース

- ✅ PostgreSQL
- ✅ Prisma ORM
- ✅ マイグレーション実行済み

### バックエンド

- ✅ tRPC（型安全なAPI）
- ✅ Zod（バリデーション）
- ✅ Next.js Server Components

### フロントエンド

- ✅ Next.js 16 (App Router)
- ✅ React 19
- ✅ TypeScript 5
- ✅ Tailwind CSS 4
- ✅ shadcn/ui
- ✅ React Hook Form
- ✅ TanStack Query (via tRPC)

### 認証

- ✅ NextAuth.js (Auth.js v5)

## デバッグ情報

### データベース確認

```bash
# Prisma Studio でデータベースを確認
pnpm prisma studio
```

### ログ確認

- ブラウザのコンソール
- 開発サーバーのターミナル出力

## 既知の問題

1. **利用者詳細画面が未実装**
   - 利用者一覧から詳細画面に遷移できるが、画面が存在しない
   - 優先度: High

2. **その他の機能画面が未実装**
   - 支援記録、バイタルサイン、服薬管理、申し送り、ヒヤリハットの画面
   - 優先度: Medium

## まとめ

現在の実装状況：

- ✅ データベース設計完了
- ✅ tRPCルーター完全実装
- ✅ 利用者管理の基本機能実装
- ⏳ その他の機能画面は実装待ち

次のステップ：

1. 利用者詳細画面の実装
2. 支援記録入力画面の実装
3. バイタルサイン記録・グラフ表示の実装

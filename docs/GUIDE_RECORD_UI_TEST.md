# ガイド記録UI 動作確認チェックリスト

**作成日**: 2026-03-03
**目的**: FR-011-UI（ガイド記録UI）の動作確認とMVP達成度の更新
**対象**: ガイド記録の作成〜提出〜実績生成の全フロー

---

## 🎯 動作確認の目的

1. DRAFT→SUBMITTED状態遷移が正常に動作することを確認
2. ServiceRecord自動生成が正常に動作することを確認
3. レビュー済みマーク機能が正常に動作することを確認
4. 権限制御が正しく機能することを確認
5. 印刷機能が正常に動作することを確認

**期待される結果**: MVP達成度が **60% → 80%** に上昇

---

## ✅ 事前準備

### 1. 開発サーバーの起動確認

- [x] 開発サーバーが起動している（`https://localhost:3443`）
- [ ] データベース接続が正常
- [ ] 認証が正常に動作している

### 2. テストユーザーの準備

以下の3種類のロールでテストを実施:

| ロール  | メール              | 用途                             |
| ------- | ------------------- | -------------------------------- |
| STAFF   | staff@example.com   | DRAFT作成・提出のみ              |
| LEAD    | lead@example.com    | 全記録編集可・レビュー済みマーク |
| MANAGER | manager@example.com | 全記録編集可・レビュー済みマーク |

### 3. テスト用利用者の準備

- [ ] テスト用利用者を1名作成（例: 山田太郎）
- [ ] 利用者IDを控える

---

## 📋 動作確認チェックリスト

### Test 1: DRAFT状態のガイド記録作成（STAFF）

**前提条件**:

- ログインユーザー: STAFF
- 対象利用者: 山田太郎

**手順**:

1. [ ] `/recipients/[id]` から利用者詳細を表示
2. [ ] 「ガイド記録」タブまたはリンクをクリック
3. [ ] `/recipients/[id]/guide` でガイド記録一覧を表示
4. [ ] 「ガイド記録登録」ボタンをクリック
5. [ ] `/recipients/[id]/guide/new` で作成フォームを表示
6. [ ] 必須項目を入力:
   - 開始日時: 2026-03-03 10:00
   - 終了日時: 2026-03-03 12:00
   - 外出先: 〇〇公園
   - 目的: 散歩
   - 移動手段: 徒歩
   - 支援内容: 散歩の付き添い
7. [ ] 「下書き保存」ボタンをクリック
8. [ ] 一覧画面にリダイレクトされる
9. [ ] 作成した記録が表示される
10. [ ] 状態バッジが「下書き」（灰色）で表示される

**期待される結果**:

- ✅ DRAFT状態のガイド記録が作成される
- ✅ 一覧画面に「下書き」バッジ付きで表示される
- ✅ 「編集」「提出」「削除」ボタンが表示される

**実際の結果**:

```
記録日時:
状態バッジ:
ボタン表示:
```

**結果**: ⬜ 成功 / ⬜ 失敗

---

### Test 2: DRAFT→SUBMITTED遷移（STAFF）

**前提条件**:

- Test 1で作成したDRAFT記録が存在
- ログインユーザー: STAFF

**手順**:

1. [ ] `/recipients/[id]/guide` でガイド記録一覧を表示
2. [ ] 作成した記録の「提出」ボタンをクリック
3. [ ] 確認ダイアログが表示される
4. [ ] 「OK」をクリック
5. [ ] 状態バッジが「提出済み」（青色）に変わる
6. [ ] 「編集」ボタンが非表示または無効化される
7. [ ] 「提出」ボタンが非表示になる

**期待される結果**:

- ✅ DRAFT→SUBMITTEDに状態遷移する
- ✅ バッジが「提出済み」に変わる
- ✅ 編集ボタンが使用不可になる
- ✅ `submittedAt`, `submittedBy`が記録される

**実際の結果**:

```
状態遷移:
バッジ表示:
ボタン制御:
submittedAt:
submittedBy:
```

**結果**: ⬜ 成功 / ⬜ 失敗

---

### Test 3: ServiceRecord自動生成の確認

**前提条件**:

- Test 2でSUBMITTED状態に遷移した記録が存在

**手順**:

1. [ ] `/service-records` にアクセス
2. [ ] サービス提供実績一覧を表示
3. [ ] Test 2で提出した記録に対応するServiceRecordを探す
4. [ ] ServiceRecordをクリックして詳細を表示
5. [ ] 以下の項目が正しく同期されているか確認:
   - serviceDate: ガイド記録の開始日
   - startTime: ガイド記録の開始時刻
   - endTime: ガイド記録の終了時刻
   - duration: 自動計算された時間（分）
   - destination: 外出先
   - purpose: 目的
   - serviceDetail: 支援内容
   - guideRecordId: ガイド記録のIDが紐付いている

**期待される結果**:

- ✅ GuideRecordのSUBMIT時にServiceRecordが自動生成される
- ✅ guideRecordIdで1:1紐付けがされている
- ✅ データが正しく同期されている
- ✅ durationが自動計算されている（120分）

**実際の結果**:

```
ServiceRecord ID:
guideRecordId:
serviceDate:
startTime:
endTime:
duration:
destination:
purpose:
serviceDetail:
```

**結果**: ⬜ 成功 / ⬜ 失敗

---

### Test 4: レビュー済みマーク機能（LEAD/MANAGER）

**前提条件**:

- Test 2でSUBMITTED状態の記録が存在
- ログインユーザー: LEAD または MANAGER

**手順**:

1. [ ] LEADまたはMANAGERでログイン
2. [ ] `/recipients/[id]/guide` でガイド記録一覧を表示
3. [ ] SUBMITTED状態の記録に「閲覧済みにする」ボタンが表示される
4. [ ] 「閲覧済みにする」ボタンをクリック
5. [ ] 確認ダイアログが表示される
6. [ ] 「OK」をクリック
7. [ ] ボタンが「閲覧済み」バッジ（緑色）に変わる
8. [ ] `reviewedAt`, `reviewedBy`が記録される

**期待される結果**:

- ✅ LEAD/MANAGERのみ「閲覧済みにする」ボタンが表示される
- ✅ クリックで閲覧済みマークが付く
- ✅ バッジが緑色の「閲覧済み」に変わる
- ✅ reviewedAt, reviewedByが記録される

**実際の結果**:

```
LEAD/MANAGER表示:
ボタンクリック:
バッジ表示:
reviewedAt:
reviewedBy:
```

**結果**: ⬜ 成功 / ⬜ 失敗

---

### Test 5: 権限制御テスト（STAFF vs LEAD/MANAGER）

#### Test 5-1: STAFF権限（提出済み記録の編集不可）

**前提条件**:

- SUBMITTED状態の記録が存在
- ログインユーザー: STAFF

**手順**:

1. [ ] STAFFでログイン
2. [ ] `/recipients/[id]/guide` でガイド記録一覧を表示
3. [ ] SUBMITTED状態の記録の「編集」ボタンが無効化または非表示になる
4. [ ] SUBMITTED状態の記録の詳細画面にアクセスしても編集ボタンが表示されない

**期待される結果**:

- ✅ STAFFはSUBMITTED記録を編集できない
- ✅ 編集ボタンが無効化または非表示
- ✅ 理由が表示される（「提出済みの記録は編集できません」）

**実際の結果**:

```
編集ボタン表示:
理由表示:
```

**結果**: ⬜ 成功 / ⬜ 失敗

---

#### Test 5-2: LEAD/MANAGER権限（全記録編集可）

**前提条件**:

- SUBMITTED状態の記録が存在
- ログインユーザー: LEAD または MANAGER

**手順**:

1. [ ] LEADまたはMANAGERでログイン
2. [ ] `/recipients/[id]/guide` でガイド記録一覧を表示
3. [ ] SUBMITTED状態の記録にも「編集」ボタンが表示される
4. [ ] 「編集」ボタンをクリックして編集画面にアクセスできる

**期待される結果**:

- ✅ LEAD/MANAGERはSUBMITTED記録も編集できる
- ✅ 編集ボタンが有効
- ✅ 編集画面にアクセスできる

**実際の結果**:

```
編集ボタン表示:
編集画面アクセス:
```

**結果**: ⬜ 成功 / ⬜ 失敗

---

### Test 6: 印刷機能テスト

#### Test 6-1: 事務所用印刷

**前提条件**:

- SUBMITTED状態の記録が存在

**手順**:

1. [ ] `/recipients/[id]/guide/[recordId]` で詳細画面を表示
2. [ ] 「事務所用印刷」ボタンをクリック
3. [ ] 印刷プレビューが表示される
4. [ ] 以下の項目が正しく表示されるか確認:
   - 利用者氏名
   - 開始日時・終了日時
   - 外出先・目的
   - 移動手段
   - 支援内容
   - 金銭管理詳細（手順書・インシデント含む）

**期待される結果**:

- ✅ 事務所用フォーマットで印刷できる
- ✅ 金銭管理詳細（手順書・インシデント）が表示される

**実際の結果**:

```
印刷プレビュー:
表示項目:
```

**結果**: ⬜ 成功 / ⬜ 失敗

---

#### Test 6-2: 家族用印刷

**前提条件**:

- SUBMITTED状態の記録が存在

**手順**:

1. [ ] `/recipients/[id]/guide/[recordId]` で詳細画面を表示
2. [ ] 「家族用印刷」ボタンをクリック
3. [ ] 印刷プレビューが表示される
4. [ ] 事務所用より簡略化されたフォーマットで表示される
5. [ ] 金銭管理詳細（手順書・インシデント）が**非表示**になる

**期待される結果**:

- ✅ 家族用フォーマットで印刷できる
- ✅ 機密情報（手順書・インシデント）が非表示
- ✅ 基本情報（日時・外出先・目的）のみ表示

**実際の結果**:

```
印刷プレビュー:
表示項目:
機密情報の非表示:
```

**結果**: ⬜ 成功 / ⬜ 失敗

---

## 📊 動作確認結果サマリー

### 全体結果

| テスト項目                    | 結果              | 備考 |
| ----------------------------- | ----------------- | ---- |
| Test 1: DRAFT作成             | ⬜ 成功 / ⬜ 失敗 |      |
| Test 2: DRAFT→SUBMITTED遷移   | ⬜ 成功 / ⬜ 失敗 |      |
| Test 3: ServiceRecord自動生成 | ⬜ 成功 / ⬜ 失敗 |      |
| Test 4: レビュー済みマーク    | ⬜ 成功 / ⬜ 失敗 |      |
| Test 5-1: STAFF権限制御       | ⬜ 成功 / ⬜ 失敗 |      |
| Test 5-2: LEAD/MANAGER権限    | ⬜ 成功 / ⬜ 失敗 |      |
| Test 6-1: 事務所用印刷        | ⬜ 成功 / ⬜ 失敗 |      |
| Test 6-2: 家族用印刷          | ⬜ 成功 / ⬜ 失敗 |      |

**成功率**: **\_ / 8 （\_\_**%）

---

## 🐛 発見された問題

### 問題1: [タイトル]

**重要度**: High / Medium / Low
**説明**:
**再現手順**:
**期待される動作**:
**実際の動作**:
**対処方法**:

---

## ✅ 次のアクション

### 全テスト成功の場合

1. [ ] MVP_MUST_LIST.mdを更新
   - FR-011-UIを「✅ 実装済み」に変更
   - MVP達成度を60% → 80%に更新
   - 基準2「ガイド記録提出→実績生成」を✅に変更
2. [ ] REQUIREMENTS.mdを更新
   - FR-011の実装状況を更新
3. [ ] 次の優先タスク（DR-004: アクセスログUI）に進む

### 一部失敗の場合

1. [ ] 失敗したテストの問題を記録
2. [ ] 優先度を判断（Blocker / High / Medium / Low）
3. [ ] Blockerの場合は即座に修正
4. [ ] High以下の場合はIssue化して後回し

---

**テスト実施者**:
**テスト実施日**:
**最終更新日**: 2026-03-03

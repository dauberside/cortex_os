## サービス提供記録帳票 — 実装確定メモ

### 確定済み実装状態（2026-03-18 時点）

| 列 | 実装内容 | 状態 |
|----|----------|------|
| 食事（朝・昼・夕） | シフト時間から自動判定 | 正式実装 |
| 入浴 | `bathDone` | 正式実装 |
| 外出 | `GuideRecord(status=SUBMITTED)` 優先、なければ `outingDone` 補完 | 正式実装 |
| 帳票形式 | 1日1行・固定列（月次・利用者別） | 正式実装 |

外出欄は以前「暫定実装」と記録していたが、GuideRecord優先の正式実装に移行完了。
「暫定」は解消済み。

### 外出判定ロジックの責務分離

- **正**: `GuideRecord(status=SUBMITTED)` の存在
- **補完**: `outingDone`（GuideRecord がない場合のみ使用）

ただの boolean ではなく、責務が分離されている。

### 帳票仕様（現状確定）

- 月次・利用者別のサービス提供記録票
- 1日1行、固定列構成
- 主要列（食事・入浴・外出）はほぼ完成

### テスト担保（2026-03-18 追加）

- `guideOutingDates` が JST 基準で正しい日付になること（UTC前日 → JST当日の境界）
- GuideRecord がある日は `outingDone=false` でも `guideOutingDates` に含まれること

### 残タスク

なし（実装・仕様・テストが揃った状態）
